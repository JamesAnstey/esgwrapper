This directory is for storing backups of configuration files used by the publisher.

`server_sync.py` is convenient to copy files from the server, for example:
```
../server_sync.py get -p /home/acrnpub/.esg/ esg.yaml
```

